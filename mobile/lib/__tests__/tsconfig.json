{
  "_comment": "Minimal tsconfig that exists solely to halt vite's tsconfig discovery walk before it reaches mobile/tsconfig.json (which extends expo/tsconfig.base, requiring mobile/node_modules — not installed in CI's root-only npm ci). The pure-helper tests under this dir use only relative imports and don't need any path aliases or expo types; just enough TS settings for esbuild to transform them.",
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "strict": true,
    "isolatedModules": true
  }
}
