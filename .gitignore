# Node / Next
node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Supabase local
supabase/.branches
supabase/.temp

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Scraper artifacts
.cache/
scraper-fixtures/*.html
!scraper-fixtures/README.md

# MAP (Murder Accountability Project) upstream artifacts — licensing
# clarification pending with Tom Hargrove (see
# docs/integrations/map-ingestion-plan.md §1). Synthetic fixtures
# (montana_sample.csv, agencies_ori_montana_sample.json) are checked in.
# Derived per-state ORI maps that pair MAP-CSV agency names with public
# city/county-seat coordinates are also held back until the license clears.
data/map/shr_*.csv
data/map/MAPdefinitionsSHR.pdf
data/map/agencies_ori_*_full.json
data/map/agencies_ori_national.json

# Coverage
coverage/
.nyc_output/
.vercel

# Claude Code workspace (per-machine task lock + scheduling state)
.claude/

# Stray root eas.json. The real EAS config lives at mobile/eas.json; a `eas`
# command run from the repo root will spawn a stub here that we don't want
# tracked.
/eas.json
