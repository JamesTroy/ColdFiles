# Node / Next
node_modules/
.next/
out/
build/
dist/
.turbo/
*.tsbuildinfo
next-env.d.ts

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Supabase local
supabase/.branches
supabase/.temp

# Editors / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Scraper artifacts
.cache/
scraper-fixtures/*.html
!scraper-fixtures/README.md

# Deno (scrape-cli + edge function local runs regenerate this; not the source of
# truth for deployed deps)
deno.lock

# Coverage
coverage/
.nyc_output/
.vercel

# Claude Code workspace (per-machine task lock + scheduling state)
.claude/

# Stray root eas.json. The real EAS config lives at mobile/eas.json; a `eas`
# command run from the repo root will spawn a stub here that we don't want
# tracked.
/eas.json
